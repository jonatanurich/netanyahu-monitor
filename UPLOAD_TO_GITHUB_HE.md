# בדיוק מה לעשות

1. חלץ את קובץ ה-ZIP.
2. פתח GitHub וצור Repository חדש.
3. לחץ `Add file` ואז `Upload files`.
4. גרור **את כל הקבצים שבתוך התיקייה**, לא את התיקייה החיצונית עצמה.
5. ודא שאתה רואה גם:
   - `.github`
   - `.env.example`
   - `.gitignore`
6. לחץ `Commit changes`.
7. אל תעלה קובץ `.env` ואל תכתוב token בתוך `main.py`.

לאחר ההעלאה, חבר את ה-Repository ל-Render באמצעות `New → Blueprint`.
